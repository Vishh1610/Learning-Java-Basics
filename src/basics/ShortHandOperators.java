package basics;

public class ShortHandOperators {
    public static void main(String[] args) {
        int i = 5;

        i += 2; // i + 2
        //+=,*=,-=,/=
        // so in using shorthand operators help us to resolve type casting problems.
        System.out.println(i);
    }
}
